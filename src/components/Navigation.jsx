import {Container, Typography} from "@mui/material";

const Navigation = () => {
    return <Container maxWidth="xl">
        <Typography>Navigation bar section</Typography>
    </Container>

}

export default Navigation;